import { RunsWicketsDeclaredPipe } from './runs-wickets-declared.pipe';

describe('RunsWicketsDeclaredPipe', () => {
  it('create an instance', () => {
    const pipe = new RunsWicketsDeclaredPipe();
    expect(pipe).toBeTruthy();
  });
});
